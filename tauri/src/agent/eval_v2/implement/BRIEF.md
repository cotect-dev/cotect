# Implement

Add new functionality to existing code.

## Design principles

- The model must integrate new features into an **existing codebase**, not write from scratch.
- New code must fit the existing patterns, style, and architecture.
- Verification: provided tests (that the model does NOT see) exercise the new functionality and must pass.
- Edge cases matter — the hidden tests cover boundaries the prompt doesn't explicitly mention.
