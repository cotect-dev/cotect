// Shared layout constants for the CodeNode editor and its overlays.

export const MIN_CODE_NODE_WIDTH = 280
/** Vertical space reserved outside the editor (header, padding) when capping
 *  the editor/preview height to the viewport. */
export const CODE_NODE_HEIGHT_RESERVED = 120
/** `.cm-content` top padding — overlays anchored to line tops add this. */
export const CM_PAD_TOP = 4
/** Horizontal gap between the editor and the right-side import-ref column. */
export const REF_GAP = 16
/** Height of a hunk accept/comment button row. */
export const HUNK_BTN_H = 20
