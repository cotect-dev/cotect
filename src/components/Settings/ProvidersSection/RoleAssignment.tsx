import type { Provider, ActiveAssignment } from '@/services/db'
import type { DetectedView } from './types'
export default function RoleAssignment(_p: { provider: Provider; detected: DetectedView | null; assignment: ActiveAssignment | null }) {
  return null
}
