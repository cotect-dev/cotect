import type { Provider, ActiveAssignment } from '@/services/db'
import type { DetectedView } from './types'
export default function ModelTable(_p: { provider: Provider; detected: DetectedView; assignment: ActiveAssignment | null }) {
  return null
}
