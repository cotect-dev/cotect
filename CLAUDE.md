# Project conventions

## Git commits

- **Never add a `Co-Authored-By:` trailer** (no `Co-Authored-By: Claude ...`,
  no co-author lines of any kind). The user authors and owns every commit;
  do not annotate authorship in commit messages.
